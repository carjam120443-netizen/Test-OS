#ifndef TEST_OS_DESKTOP_H
#define TEST_OS_DESKTOP_H

#include <stdint.h>

/* Initialize and draw the first native Test-OS graphical desktop. */
void desktop_init(void);
void desktop_redraw(void);

#endif
